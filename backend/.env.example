# Porta do servidor Express
PORT=3000

# PostgreSQL (rodando via Docker na maquina local)
# DB_HOST=localhost porque o Node roda FORA do Docker.
# Se o Node tambem estivesse dentro do Docker, seria DB_HOST=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=orquidario_yojo
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=orquidario-yojo-secret-2024
JWT_EXPIRES_IN=24h

# WhatsApp do dono da loja
WHATSAPP_NUMBER=5511998726880
